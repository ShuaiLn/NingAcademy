NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
SETUP_TOKEN=

# Server-only HTTPS endpoint on the game deployment. It receives the
# one-time ticket field in a POST body and exchanges it for a game session.
GAME_LAUNCH_EXCHANGE_URL=
